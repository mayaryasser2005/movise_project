import 'package:flutter/cupertino.dart';

class ListWatch extends StatelessWidget {
  static const String routeNamed = "ListWatch";

  const ListWatch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
